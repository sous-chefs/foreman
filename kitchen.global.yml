---
provisioner:
  name: chef_infra
  product_name: chef
  product_version: <%= ENV.fetch('CHEF_VERSION', 'latest') %>
  channel: stable
  chef_license: accept
  deprecations_as_errors: true

verifier:
  name: inspec
  sudo: true

platforms:
  - name: debian-12
  - name: ubuntu-22.04
