---
# TFTP management
:enabled: <%= @module_enabled %>
:tftproot: <%= node['foreman-proxy']['tftp_root'] %>
# Defines the TFTP Servername to use, overrides the name in the subnet declaration
<%= '#' if node['foreman-proxy']['tftp_servername'].nil? %>:tftp_servername: <%= node['foreman-proxy']['tftp_servername'] %>
