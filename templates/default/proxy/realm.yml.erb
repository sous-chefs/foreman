---
# Manage joining realms e.g. FreeIPA
:enabled: <%= @module_enabled %>

# Available providers:
#   freeipa
:realm_provider: <%= node['foreman-proxy']['realm_provider'] %>

# Authentication for Kerberos-based Realms
:realm_keytab: <%= node['foreman-proxy']['realm_keytab'] %>
:realm_principal: <%= node['foreman-proxy']['realm_principal'] %>

# FreeIPA specific settings
# Remove from DNS when deleting the FreeIPA entry
:freeipa_remove_dns: <%= node['foreman-proxy']['freeipa_remove_dns'] %>
